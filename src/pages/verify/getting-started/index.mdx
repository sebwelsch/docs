---
product: verify
category: Getting Started
sort: 0
title: Get started with Idura Verify
---

import { isIndexPage } from '../../../utils';

import { graphql as gatsbyGraphql, Link } from 'gatsby';
export const pageQuery = gatsbyGraphql`
  query GettingStarted {
    pages: allMdx(
      filter: {
        frontmatter: {
          product: { eq: "verify" }
          category: { eq: "Getting Started" }
        }
      }
      sort: {frontmatter: {sort: ASC}}
    ) {
      edges {
        node {
          __typename
          id
          frontmatter {
            title
            category
            subtitle
          }
          fields {
            slug
          }
          internal {
            contentFilePath
          }
        }
      }
    }
  }
`;

Below you will find a list of topics to get you started with Idura Verify:

<ul>
  {props.data.pages.edges
    .map(edge => edge.node)
    .filter(node => !isIndexPage(node))
    .map(node => (
      <li key={node.id}>
        <Link to={`/${node.fields.slug}/`}>{node.frontmatter.title}</Link>
        <br />
        {node.frontmatter.subtitle}
      </li>
    ))}
</ul>
